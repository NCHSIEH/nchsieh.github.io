# Firebase 設定步驟

網站已經改寫成真正串接 Firebase 的版本，但**在你完成下面幾個步驟之前，登入與審核功能不會運作**。

專案是既有的 `nchsieh-lms`，設定值已經填在 `assets/js/config.js`，不需要重新建立專案。

全部做完大約 15 分鐘。請依序執行。

---

## 步驟 1｜啟用「電子郵件／密碼」登入

1. 開啟 [Firebase Console](https://console.firebase.google.com/)，選擇專案 **nchsieh-lms**
2. 左側選單 → **Authentication**
3. 若是第一次進入，點 **開始使用 / Get started**
4. 切到 **Sign-in method** 分頁
5. 找到 **電子郵件/密碼（Email/Password）**，點進去
6. 把第一個開關 **啟用（Enable）** 打開 → **儲存**

> 第二個開關「電子郵件連結（無密碼登入）」不需要開啟。

**沒做這步會怎樣**：註冊時出現「Firebase 尚未啟用電子郵件／密碼登入方式」。

---

## 步驟 2｜加入授權網域

1. 一樣在 **Authentication** → **Settings** 分頁
2. 找到 **Authorized domains（授權網域）**
3. 確認清單中有 **`nchsieh.github.io`**，沒有的話點 **Add domain** 加上去

> `localhost` 預設就在清單裡，本機測試不用另外加。

**沒做這步會怎樣**：在 GitHub Pages 上登入會出現「目前網域未被授權」。

---

## 步驟 3｜建立 Firestore 資料庫

新版 Console 把資料庫收在子選單裡，路徑是：

**左側「產品類別」→ 資料庫和儲存空間 → 展開後選 `Firestore`**

展開的清單會長這樣，請認明 **NoSQL 分類下的 Firestore**：

```
PostgreSQL
  ⚬ SQL Connect          ← 不是這個
NoSQL
  ⚬ Firestore            ← 就是這個 ✅
  ⚬ Realtime Database    ← 不是這個（舊產品，與 Firestore 不相容）
物件儲存空間
  ⚬ Storage              ← 本站沒用到
```

進去之後：

1. 點 **建立資料庫（Create database）**
2. 資料庫 ID 保持預設的 **`(default)`**，不要改名 —— 程式碼連的是預設資料庫
3. 位置選 **asia-east1（台灣）** 或 **asia-northeast1（東京）**
4. 規則模式選 **正式版模式（Production mode）** — 別選測試模式，測試模式 30 天後會全部鎖死
5. 建立完成

> 位置一旦選定就**無法變更**，要換只能砍掉重建，所以選之前想清楚。

---

## 步驟 4｜貼上安全性規則

這一步是整個系統的核心。規則在 Google 伺服器上執行，任何人都繞不過去。

1. 回到 **Firestore**（同步驟 3 的路徑）→ 上方 **規則（Rules）** 分頁
2. 把編輯器裡的內容 **全部刪除**
3. 打開專案裡的 `firestore.rules` 檔案，**全部複製貼上**
4. 點 **發布（Publish）**

貼上後應該看到「規則已發布」。若出現語法錯誤，通常是貼上時漏了開頭或結尾的大括號。

---

## 步驟 5｜建立你的管理者帳號

管理者是誰，直接寫在安全性規則裡（`firestore.rules` 的 `adminEmails()`），
目前設定的是 **`nchsieh@gmail.com`**。要換成別的信箱，見本節最後。

**不需要建立 `admins` 集合，也不用複製 UID。**

1. **Authentication** → **使用者（Users）** 分頁
2. 點 **新增使用者（Add user）**
3. Email 填 `nchsieh@gmail.com`，密碼自訂（至少 6 碼）
4. 建立完成

### 完成 Email 驗證

Console 手動建立的帳號預設是「**未驗證**」狀態，而管理權限要求信箱已驗證
（這樣才能確認信箱確實由你本人持有）。

1. 開啟 `admin.html`，用剛才的 Email 與密碼登入
2. 畫面會顯示「只差一步：請完成 Email 驗證」，並出現 **寄送驗證信** 按鈕
3. 點它 → 去信箱收信（記得看垃圾郵件匣）→ 點開信中的連結
4. 回到 `admin.html`，按 **我驗證好了**
5. 後台就開了

之後再登入就不必重複這段，直接進後台。

### 想換成別的管理者信箱？

兩個地方都要改，而且必須一致：

| 檔案 | 位置 |
|---|---|
| `firestore.rules` | `adminEmails()` 函式裡的陣列 → 改完要重新 **發布** |
| `assets/js/config.js` | `ADMIN_EMAILS` 陣列 → 改完要 push 到 GitHub |

只改一邊的話，會出現「進得了後台但存不了檔」（只改了 config）
或「明明有權限卻進不去」（只改了 rules）。

---

## 步驟 6｜建立第一門課程

1. 開啟 `admin.html` 登入後台 → 左側選單 **課程管理**
2. 填寫課程代碼、名稱、學期等欄位 → **建立課程**
3. 在右側課程清單點 **講義與作業**，把 PDF 拖進上傳區

### 上傳講義（拖拉方式）

後台的「講義與作業」視窗有一個拖拉區，檔案會直接送上 GitHub repo，
成功後自動建立講義紀錄，不必再手動 commit。

**第一次使用要先設定一次金鑰：**

1. 後台 → 課程管理 → 任一課程的「講義與作業」→ 右上角 **上傳設定**
2. 展開「怎麼產生金鑰？」，照著開啟 GitHub 金鑰產生頁面
3. 重點設定：
   - **Repository access** → `Only select repositories` → 只勾 `nchsieh.github.io`
   - **Permissions** → `Contents` 設為 **Read and write**
   - 其他權限全部維持 `No access`
4. 複製產生的 `github_pat_...` 貼回後台 → **儲存並測試連線**
5. 看到「連線成功」就完成了

金鑰只存在你這台電腦的瀏覽器裡，不會寫進程式碼，也不會被其他訪客拿到。
即使外洩，它能做的也只有修改這個公開網站的內容，碰不到你其他的儲存庫。

> 金鑰有有效期限（建議設 90 天或 1 年）。到期後上傳會顯示「金鑰無效或已過期」，
> 重新產生一組貼上即可。

### PPTX 請先轉成 PDF

拖入 `.pptx` 或 `.ppt` 時，系統會擋下來並顯示轉檔步驟。這是刻意的：

- PPTX 需要學生自備 PowerPoint，手機上幾乎打不開
- 不同版本之間字型與版面容易跑掉
- PDF 任何裝置都能開，版面不會變

PowerPoint 匯出只要幾秒：**檔案 → 另存新檔 → 檔案類型選 PDF**。
（Mac：檔案 → 匯出 → 格式選 PDF；Google 簡報：檔案 → 下載 → PDF 文件）

轉檔交給 PowerPoint 自己做，字型與版面的還原度比任何伺服器端轉檔工具都好。

### 其他注意事項

- 單檔上限 **25 MB**。超過的話請壓縮，或放雲端硬碟後用「手動填寫」貼外部連結
- 檔案會放在 `assets/courses/課程代碼/單元名稱/` 底下，路徑自動產生
- 同名檔案會覆寫，方便更新講義版本

> **重要提醒**：放在 GitHub repo 的檔案本身是公開的，任何知道完整網址的人都能下載。
> 審核機制控制的是「誰看得到講義清單」，不是檔案本身的存取權。
> 若有不宜外流的講義，請勿放入 repo。

---

## 學生端流程長什麼樣

1. 學生在課程頁點「學生登入」→ 切到「申請新帳號」→ 填 Email、密碼、姓名、學號、班級
2. 系統會建立一筆 `status: "pending"` 的紀錄，並盡量寄一封驗證信（但收不到不影響後續流程）
3. **你**在後台 → 學生管理 → 審核佇列 → 看到這筆申請，核對姓名／學號／班級後點「核准」
   （左側選單與分頁上都會顯示待審核數量的紅點）
4. 學生重新整理頁面，就能看到講義清單與作業截止時間

存取條件是：**已登入 + 狀態為 approved**。這是 `firestore.rules` 裡強制的。

**為什麼拿掉了 Email 驗證這個條件：** 早期設計要求學生點開驗證信才能通過審核，
但驗證信常被學校信箱系統當垃圾信擋掉，導致學生完全卡關、永遠進不來。改成不強制驗證後，
審核的把關責任落在你身上——核准前務必核對申請人的姓名、學號、班級是否合理，
不要看到申請就照單全收。對小班制課程網站來說，這個取捨是合理的；如果你的修課人數很多、
彼此不認識、無法逐一核對身分，才需要考慮找其他方式加強驗證。

---

## 排查常見問題

| 症狀 | 原因與處理 |
|---|---|
| 「Firebase 尚未啟用電子郵件／密碼登入方式」 | 步驟 1 沒做 |
| 「目前網域未被授權」 | 步驟 2 沒做，或網域拼錯 |
| 課程頁一直顯示「課程讀取失敗」 | 步驟 3 沒建立 Firestore（或誤建成 Realtime Database），或步驟 4 規則沒發布 |
| 「這個帳號不在管理者名單中」 | 登入的 Email 與 `adminEmails()` 不符。看訊息裡顯示的 Email 對照 |
| 進得了後台但一存檔就權限不足 | `config.js` 與 `firestore.rules` 的名單不一致，或規則沒重新發布 |
| 學生已核准但還是看不到講義 | 檢查「學生管理」的「可看課程」有沒有不小心限定範圍；或該學生的狀態其實是 suspended（已暫停）而非 approved |
| 收不到驗證信 | 不影響審核與使用，純粹是想讓學生順便驗證信箱的話才需要處理，見下方「收不到驗證信怎麼辦」專節 |
| 改了規則但行為沒變 | 規則發布後約 1 分鐘生效，並請強制重新整理（Ctrl+Shift+R） |

---

## 收不到驗證信怎麼辦

**重要更新：Email 驗證現在不是存取講義的必要條件**——學生只要申請被核准，就算沒收到或沒點驗證信，
一樣能正常登入、看到講義。這一節純粹是給「想讓學生順便完成信箱驗證」的情境參考，不處理也完全不影響使用。

如果還是想排查為什麼收不到驗證信，照下面順序看。這是 Firebase 免費方案最常被問到的問題，
多數情況不是程式的錯，而是信件在寄送或收信端被擋下。

1. **先查垃圾郵件／促銷／病毒隔離匣**
   Firebase 的驗證信預設由 `noreply@<你的專案 id>.firebaseapp.com` 寄出，對很多信箱（尤其學校信箱）來說是「陌生寄件者」，
   很容易被規則式過濾器擋到垃圾郵件，甚至完全不通知、直接隔離。這是目前最常見的原因，請先確認學生真的仔細找過垃圾郵件匣。

2. **確認學校信箱沒有更嚴格的白名單機制**
   有些大學信箱系統（Office 365 / Google Workspace 教育版）由資訊處統一設定「僅允許白名單網域寄信」，
   一般使用者自己看不到、也改不了這個設定。如果同一批學生用 Gmail 之類的私人信箱重新申請一次能收到信，
   而學校信箱收不到，就可以確定是學校端擋信，須請學生改用私人信箱申請，或請學校資訊處把
   `firebaseapp.com`（或你在下方設定的自訂寄件人）加入白名單。

3. **把寄件人「顯示名稱」改成看起來像本人／本課程，而不是系統預設**
   免費方案雖然不能換成自己的網域寄信（那需要有網域的 DNS 管理權限），但可以免費客製化「寄件人顯示名稱」與「回覆地址」，
   讓信件在收件匣看起來更像正常來信，降低被過濾的機率：
   - Firebase Console → **Authentication** → **Templates**
   - 點選「Email address verification」旁邊的鉛筆圖示
   - 把 **Sender name** 改成例如「謝楠楨教授｜課程系統」，**Reply to** 填你自己的信箱
   - 存檔即時生效，不需要改程式碼

4. **請學生用「重寄驗證信」，但不要短時間內連續按**
   Firebase 對同一帳號的驗證信寄送有頻率限制，短時間內重複點擊「重寄驗證信」可能會被判定為異常而暫時擋下，
   畫面上會出現「嘗試次數過多，請稍候幾分鐘再試」──這是正常保護機制，等幾分鐘再試一次即可。

5. **確認帳號本身沒有輸入錯誤的 Email**
   請學生在登入視窗確認自己輸入的信箱有沒有打錯字；如果打錯，需要用「刪除」把這筆錯誤申請清掉，用正確信箱重新申請一次
   （Firebase Authentication 裡對應的帳號需另外到 Console → Authentication → Users 手動刪除，才能用同一個 Email 重新註冊）。

6. **確認沒有超過每日寄信上限**
   Spark（免費）方案的驗證信上限是每日 1000 封，對一門課數十位學生的規模幾乎不可能碰到；
   如果你在短時間內大量測試、重複建立又刪除帳號，才有可能短暫觸頂，等隔天額度重置即可。

若照著做完 1–4 仍然收不到，通常就是該學校信箱系統端在擋信，最務實的做法是請學生改用 Gmail 等私人信箱申請修課帳號。

---

## 費用

這套設定完全落在 Firebase 免費方案（Spark）內：

- Authentication：每月 50,000 次驗證
- Firestore：每日 50,000 次讀取、20,000 次寫入、1 GiB 儲存空間

以一門課數十位學生的規模，用量大概是免費額度的百分之一以內。**不需要綁定信用卡。**

（本站沒有使用 Firebase Storage，因此不會觸發需要升級 Blaze 方案的條件。）

---

## 資料備份

後台 → **系統** → **匯出 JSON 備份**（或右上角「匯出備份」），會下載一份包含課程、講義、作業、公告與學生名單的完整快照。建議每學期做一次。

---

## 設定公告 Email 提醒（EmailJS）

後台「公告」分頁可以在發布公告時（或事後）一鍵寄送 Email 提醒給符合條件的已核准學生。這個功能透過
[EmailJS](https://www.emailjs.com) 運作——一個完全在瀏覽器端執行的寄信服務，不需要架設後端伺服器，
免費方案每月可寄 200 封。三個欄位沒填之前，「寄送提醒」按鈕會顯示提示、不會真的送出，不影響其他功能。

**設定步驟：**

1. 到 [emailjs.com](https://www.emailjs.com) 註冊帳號
2. **Email Services** → **Add New Service**，連接你自己的信箱（Gmail、Outlook 等皆可）→ 記下產生的 **Service ID**
3. **Email Templates** → **Create New Template**，內容至少要用到以下變數（EmailJS 會在寄送時自動代入）：
   - `{{to_email}}`：收件者信箱（必填，通常設在範本的「To Email」欄位）
   - `{{to_name}}`：收件者姓名
   - `{{subject}}`：公告標題
   - `{{message}}`：公告內容
   儲存後記下 **Template ID**
4. **Account** → **General**，找到 **Public Key**
5. 把三組值填進 `assets/js/config.js`：
   ```js
   export const EMAILJS_SERVICE_ID  = '你的 Service ID';
   export const EMAILJS_TEMPLATE_ID = '你的 Template ID';
   export const EMAILJS_PUBLIC_KEY  = '你的 Public Key';
   ```
6. 存檔、push 到 GitHub，重新整理後台的「系統」分頁，應會看到「Email 提醒（EmailJS）」顯示為已設定

**使用方式：** 發布公告時勾選「發布後立即寄送 Email 提醒」，或事後在公告列表按「寄送提醒」。若公告有指定班級，
只會寄給該班級、且已核准的學生；沒有指定班級則寄給全部已核准學生。寄送狀態與時間會記錄在公告列表中，避免重複寄送。

**注意事項：**
- 這組金鑰會出現在前端程式碼裡（任何造訪網站的人都看得到），但 EmailJS 的 Public Key 本來就設計成可以公開，
  真正的信箱帳密留在 EmailJS 後台，不會外流。
- 免費方案每月 200 封，一次公告寄給數十位學生完全夠用；若學生人數很多、公告又頻繁，才需要考慮升級 EmailJS 付費方案。
- 這個功能與 Firebase Auth 的驗證信是兩條完全獨立的管道，互不影響。

---

## 刪除學生時一併刪除 Firebase 帳號

後台「學生管理」按「刪除」預設只會移除 Firestore 裡的申請紀錄，Firebase Authentication 裡的帳號本身
不會被刪除——這是刻意的安全限制：瀏覽器端的 Firebase SDK 沒有辦法讓管理者刪除「別人」的帳號，
只能刪除「目前登入中那個人自己」的帳號。要讓管理者真的能刪除別人的帳號，必須用 Firebase **Admin SDK**，
而它的金鑰權限極高（能刪除/新增任何帳號、讀寫所有資料），絕對不能放進瀏覽器程式碼裡，所以需要另外
一個小小的伺服器來保管這把金鑰。

這裡用的做法：在 [Vercel](https://vercel.com) 上部署一支免費的 serverless function（程式已經寫好，
在專案的 `admin-api/` 資料夾），金鑰只存在 Vercel 的環境變數裡，主網站的前端程式碼完全碰不到它，
只會呼叫這支函式的網址。Vercel 的免費方案（Hobby）不需要信用卡，每月 100 萬次呼叫，這個功能一個月
用不到幾次，完全在額度內。

如果不想處理這一段，可以完全跳過——不設定的話，「刪除」會維持現在的行為（只刪 Firestore 紀錄），
不影響網站其他任何功能。

**設定步驟：**

1. **取得 Firebase 服務帳戶金鑰**
   Firebase Console → 齒輪圖示 → **專案設定** → **服務帳戶** → **產生新的私密金鑰**，
   會下載一個 JSON 檔案。**這個檔案要妥善保管，不要放進 GitHub、不要分享給任何人**——
   它等於是你整個 Firebase 專案的最高權限鑰匙。

2. **安裝並登入 Vercel CLI**（電腦需要先裝好 [Node.js](https://nodejs.org)）
   ```powershell
   npm install -g vercel
   vercel login
   ```
   `vercel login` 會開瀏覽器讓你用 GitHub 或 Email 登入/註冊 Vercel 帳號，免費、不需要信用卡。

3. **部署**
   ```powershell
   cd "D:\Documents\Personal Resume\NCHSIEH WEB\admin-api"
   npm install
   vercel --prod
   ```
   過程中會問幾個問題（要不要連結既有專案、專案名稱等），第一次的話全部按 Enter 用預設值即可。
   部署完成後終端機會印出一個網址，例如 `https://nchsieh-admin-api-xxxx.vercel.app`，記下來。

4. **設定環境變數**
   到 [vercel.com](https://vercel.com) 網頁版 → 剛剛那個專案 → **Settings** → **Environment Variables**，新增兩筆：

   | Key | Value |
   |---|---|
   | `FIREBASE_SERVICE_ACCOUNT_KEY` | 步驟 1 下載的 JSON 檔案，**整個檔案內容**貼進去（一行也沒關係） |
   | `ADMIN_EMAILS` | 管理者信箱，例如 `nchsieh@gmail.com`，多筆用逗號分隔 |

   存檔後回到終端機重新部署一次讓環境變數生效：
   ```powershell
   vercel --prod
   ```

5. **把網址填進網站設定**
   打開 `assets/js/config.js`，把步驟 3 拿到的網址加上 `/api/delete-student` 填進去：
   ```js
   export const DELETE_USER_API_URL = 'https://nchsieh-admin-api-xxxx.vercel.app/api/delete-student';
   ```
   存檔、push 到 GitHub。重新整理後台的「系統」分頁，應該會看到「刪除學生時一併刪除登入帳號」顯示為已設定。

**使用方式：** 設定完成後，「學生管理」裡按「刪除」的確認訊息會改成「這會同時刪除 Firestore 紀錄與
Firebase 登入帳號」，按下確定後兩邊都會清除。如果 Auth 帳號刪除失敗（例如 Vercel 那邊暫時連不上），
Firestore 紀錄還是會照樣刪掉，畫面會用黃色警示告訴你「請至 Console 手動刪除」，不會因此卡住整個操作。

**安全機制：** 這支 API 不是誰都能呼叫——它會先驗證呼叫者附上的 Firebase 登入憑證是不是真的、
Email 有沒有驗證過、而且信箱有沒有在 `ADMIN_EMAILS` 名單內，都通過才會真的執行刪除。
光是知道這支 API 的網址沒有用，沒有合法的管理者登入身分一樣會被擋下來。

**注意事項：**
- `admin-api/` 是獨立的小型 Node.js 專案，跟主網站（GitHub Pages）分開部署，兩者互不影響。
- Vercel 的環境變數是加密保存、不會出現在程式碼或 Git 紀錄裡。
- 如果之後要換人管理，或覺得這把服務帳戶金鑰有外流疑慮，可以到 Firebase Console → 服務帳戶頁面把舊金鑰刪掉、重新產生一組，並更新 Vercel 的環境變數。
